import React from "react";

const Img = (img) => {
  // console.log(img);
  img = img.img;
  // console.log(img);
  
  return (
    <div>
      {/* {img.map((img) => (
        <img src={img} alt={img.file_path} className="actor__img" />
      ))} */}
    </div>
  );
};

export default Img;
